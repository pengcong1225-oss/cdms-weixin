<view class="choice-group">
  <view
    wx:for="{{options}}"
    wx:key="value"
    class="choice-tile"
    data-value="{{item.value}}"
    bindtap="change"
  >
    <view class="choice-title">{{item.label}}</view>
    <view wx:if="{{item.caption}}" class="choice-caption">{{item.caption}}</view>
  </view>
</view>
